<ul class="media-list">
  <% posts.each do |post| %>
    <li class="media">
      <div class="post media-body">
        <div class="created-date">
          <span class="text-muted">posted at <%= post.created_at %> by <%= post.user.name %></span>
        </div>
        <div class="post-image text-center">
          <% if post.image? %>
            <%= image_tag post.image.url, class: "media-object " %>
          <% else %>
            <%= image_tag 'noimage.gif' %>
          <% end %>
        </div>
        <div class="post-content text-center">
          <%= post.content%>
        </div>
        <% if logged_in? %>
          <div class="post-buttons text-right">
            <%= render "interests/agree_button", post: post %>
            <%= post.agrees.count %>
            <%= render "interests/disagree_button", post: post %>
            <%= post.disagrees.count %>
          </div>
        <% end %>
      </div>
    </li>
  <% end %>
</ul>